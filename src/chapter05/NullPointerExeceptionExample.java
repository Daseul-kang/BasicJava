package chapter05;

import java.util.Random;

public class NullPointerExeceptionExample {
    public static void main(String[] args) {
        //int[] intArray = null;
        //intArray[0] -= 10;

//        Random random = null;
 //       int num = random.nextInt(6);
        //주소가 저장되어있지 않아서 예외(에러)가 뜸.
//        System.out.println(num);

//        String str = null;
//        System.out.println("총 문자수: " + str.length());
      //  run(null);
    }
   // private static  void run(Random random) {
     //   random.nextInt(6);
    // 변수의 초기화 값이 null이면 (.)을 찍고 할 수가 없다(?)
 }


